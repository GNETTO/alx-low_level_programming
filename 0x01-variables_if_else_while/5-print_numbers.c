#include <stdio.h>

/**
 * main - list decimal base
 * Description: look above
 * Return: return always 0 (success)
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf(i);
		i++;
	}
	printf("\n");
	return (0);
}
