#include <stdio.h>

/**
 * main - decimal
 * Description: description here
 * Return: return 0 (success)
 */

int main(void)
{
	int ch;

	for (ch = 48; ch <= 58; ch++)
		printchar(ch);
	printchar('\n');
	return (0);
}
